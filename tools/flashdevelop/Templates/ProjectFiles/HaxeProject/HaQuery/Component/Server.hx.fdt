package $(Package);

import haquery.server.Lib;

class Server extends BaseServer
{
	function init()
    {
        if (!Lib.isPostback)
        {
            $(EntryPoint)
        }
        else
        {
            
        }
	}
	
	function preRender()
    {
        
	}
}
