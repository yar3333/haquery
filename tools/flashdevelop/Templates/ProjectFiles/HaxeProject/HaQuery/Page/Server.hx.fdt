#if php

package $(Package);

import haquery.server.Lib;
import haquery.server.HaqPage;

class $(FileName) extends HaqPage
{
	function init()
    {
        if (!Lib.isPostback)
        {
            $(EntryPoint)
        }
        else
        {
            
        }
	}
	
	function preRender()
    {
        
    }
}

#end