package $(Package);

import haquery.server.Lib;
import haquery.server.HaqPage;

class $(FileName) extends HaqPage
{
    //var template : TemplateServer;
    
    function init()
    {
        if (!Lib.isPostback)
        {
            $(EntryPoint)
        }
        else
        {
            
        }
    }
    
    function preRender()
    {
        
    }
}
